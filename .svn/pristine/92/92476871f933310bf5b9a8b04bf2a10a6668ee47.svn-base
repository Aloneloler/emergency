/**
 * Copyright(C) 2019,by xskj,All rights reserved
 *
 * @date 2019年06月27日
 * @author zs
 * @desc
 *
 */
import React, { PureComponent } from 'react';

class TablePage extends PureComponent {
  render() {
    return (
      <DTable
            // border="1px solid #000"
            width="100%"
            height="100%"
            dataSource={formatData(dataS, 'dataSource')}
            columns={formatData(dataS, 'columns')}
            align="center"
            rowHeight="60px"
            padding="10px 0"
            headerStyle={{
              fontSize: 12,
              // color: '#000',
              // background: 'transparent',
            }}
            contentStyle={{
              // fontSize: 12,
              // color: '#383838',
              // background: 'transparent',
            }}
            hoverStyle={{
              background:"#789",
              color:"#fff"
            }}
            whiteSpace="nowrap"
            // horizontalSpacing='5px solid #333'
            verticalSpacing ='5px solid #FBFBFB'
            // scrollBackground="#666"
          />
    );
  }
}

export default TablePage;
